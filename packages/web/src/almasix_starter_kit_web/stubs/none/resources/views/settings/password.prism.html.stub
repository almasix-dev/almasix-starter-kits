@extends('layouts.app')
@section('title', 'Password — {{ app_display }}')
@section('content')
  <nav class="settings-nav">
    <a href="{{ route('profile.show') }}">Profile</a>
    <a href="{{ route('user-password.edit') }}">Password</a>
    <a href="{{ route('two-factor.show') }}">Two-factor</a>
    <a href="{{ route('appearance.edit') }}">Appearance</a>
  </nav>

  <h1 class="font-display" style="font-size:1.5rem;margin:0">Password</h1>
  @isset(status)<p class="alert alert-ok">{{ status }}</p>@endisset
  @isset(error)<p class="alert alert-error">{{ error }}</p>@endisset
  <form method="post" action="{{ form_action }}" style="max-width:24rem;margin-top:1rem">@csrf
    {!! method_field('PUT') !!}
    <label class="field">Current password<input type="password" name="current_password" required/></label>
    <label class="field">New password<input type="password" name="password" required/></label>
    <label class="field">Confirm password<input type="password" name="password_confirmation" required/></label>
    <button type="submit" class="btn">Update password</button>
  </form>
@endsection
