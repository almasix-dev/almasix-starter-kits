@extends('layouts.guest')
@section('title', 'Reset password — {{ app_display }}')
@section('content')
  <h1 class="font-display" style="font-size:1.35rem;margin:0">Reset password</h1>
  @isset(error)<p class="alert alert-error">{{ error }}</p>@endisset
  <form method="post" action="{{ form_action }}">@csrf
    <input type="hidden" name="token" value="{{ token }}"/>
    <label class="field">Email<input type="email" name="email" value="{{ email }}" required/></label>
    <label class="field">New password<input type="password" name="password" required/></label>
    <label class="field">Confirm password<input type="password" name="password_confirmation" required/></label>
    <button type="submit" class="btn" style="width:100%">Reset password</button>
  </form>
@endsection
