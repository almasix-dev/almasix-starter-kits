<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1"/>
  <title>@yield('title', '{{ app_display }}')</title>
  <link rel="preconnect" href="https://fonts.googleapis.com"/>
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin/>
  <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;500;600&family=Outfit:wght@400;500;600;700;800&display=swap" rel="stylesheet"/>
  <link rel="stylesheet" href="{{ asset('css/kit.css') }}"/>
  <script>(function(){try{var m=localStorage.getItem('forge-theme')||'system';var d=m==='dark'||(m==='system'&&matchMedia('(prefers-color-scheme: dark)').matches);document.documentElement.classList.toggle('dark',d);}catch(e){}})();</script>
  <script>
    window.__forgeSetTheme=function(mode){var m=mode||localStorage.getItem('forge-theme')||'system';var dark=m==='dark'||(m==='system'&&matchMedia('(prefers-color-scheme: dark)').matches);document.documentElement.classList.toggle('dark',dark);localStorage.setItem('forge-theme',m);};
  </script>
  @conduitScripts
</head>
<body>
  <div class="shell">
    <aside class="sidebar">
      <a class="brand" href="{{ route('dashboard') }}"><span class="dot"></span>{{ name }}</a>
      <nav>
        <a href="{{ route('dashboard') }}">Dashboard</a>
        <a href="{{ route('notifications.index') }}">Notifications</a>
        <a href="{{ route('profile.show') }}">Settings</a>
        <a href="{{ route('teams.index') }}">Teams</a>
      </nav>
      <div class="muted" style="padding:1rem;font-size:.75rem;border-top:1px solid var(--line)">Forge · {{ name }}</div>
    </aside>
    <div class="main">
      <header class="topbar">
        @conduit('theme_toggle')
        <details>
          <summary class="btn btn-ghost" style="list-style:none">Account</summary>
          <div style="position:absolute;right:1rem;background:var(--surface);border:1px solid var(--line);border-radius:.75rem;padding:.35rem 0;min-width:10rem;z-index:20">
            <a href="{{ route('profile.show') }}" style="display:block;padding:.4rem 1rem">Profile</a>
            <a href="{{ route('appearance.edit') }}" style="display:block;padding:.4rem 1rem">Appearance</a>
            <form method="post" action="{{ route('logout') }}">@csrf<button type="submit" class="btn-ghost" style="width:100%;text-align:left;border:0;padding:.4rem 1rem;color:#dc2626">Log out</button></form>
          </div>
        </details>
      </header>
      <main class="content">@yield('content')</main>
    </div>
  </div>
</body>
</html>
