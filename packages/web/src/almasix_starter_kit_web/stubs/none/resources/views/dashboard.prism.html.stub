@extends('layouts.app')
@section('title', 'Dashboard — {{ app_display }}')
@section('content')
  @isset(status)<p class="alert alert-ok" role="status">{{ status }}</p>@endisset
  <h1 class="font-display" style="font-size:1.75rem;margin:0">Hello, {{ user_name }}</h1>
  <p class="muted">You are signed in to {{ name }}@isset(team_name) · current team <strong>{{ team_name }}</strong>@endisset.</p>
  <div style="display:grid;gap:1rem;margin-top:1.5rem;grid-template-columns:repeat(auto-fit,minmax(14rem,1fr))">
    <a href="{{ route('profile.show') }}" style="display:block;padding:1.1rem;border:1px solid var(--line);border-radius:.75rem;background:var(--surface);color:inherit">
      <strong>Settings</strong><p class="muted" style="margin:.35rem 0 0;font-size:.875rem">Profile, password, appearance, and two-factor.</p>
    </a>
    <a href="{{ route('teams.index') }}" style="display:block;padding:1.1rem;border:1px solid var(--line);border-radius:.75rem;background:var(--surface);color:inherit">
      <strong>Teams</strong><p class="muted" style="margin:.35rem 0 0;font-size:.875rem">Create, switch, invite, and manage members.</p>
    </a>
  </div>
@endsection
