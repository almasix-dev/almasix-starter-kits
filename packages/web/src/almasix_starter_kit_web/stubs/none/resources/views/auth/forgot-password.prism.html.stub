@extends('layouts.guest')
@section('title', 'Forgot password — {{ app_display }}')
@section('content')
  <h1 class="font-display" style="font-size:1.35rem;margin:0">Forgot password</h1>
  @isset(error)<p class="alert alert-error">{{ error }}</p>@endisset
  @isset(status)<p class="alert alert-ok">{{ status }}</p>@endisset
  <form method="post" action="{{ form_action }}">@csrf
    <label class="field">Email<input type="email" name="email" required/></label>
    <button type="submit" class="btn" style="width:100%">Email reset link</button>
  </form>
  <p style="text-align:center;margin-top:1rem"><a href="{{ login_url }}">Back to login</a></p>
@endsection
