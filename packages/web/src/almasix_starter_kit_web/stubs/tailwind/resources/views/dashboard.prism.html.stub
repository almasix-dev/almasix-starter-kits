@extends('layouts.app')

@section('title', 'Dashboard — {{ app_display }}')

@section('content')
  @isset(status)
    <p class="mb-6 rounded-lg border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-800 dark:border-emerald-900 dark:bg-emerald-950/40 dark:text-emerald-200" role="status">{{ status }}</p>
  @endisset
  <h1 class="font-display text-3xl font-semibold tracking-tight">Hello, {{ user_name }}</h1>
  <p class="mt-2 text-muted">
    You are signed in to {{ name }}@isset(team_name) · current team <span class="font-medium text-ink">{{ team_name }}</span>@endisset.
  </p>
  <div class="mt-8 grid gap-4 sm:grid-cols-2">
    <a href="{{ route('profile.show') }}" class="rounded-xl border border-line bg-surface p-5 transition hover:border-brand">
      <h2 class="font-semibold">Settings</h2>
      <p class="mt-1 text-sm text-muted">Profile, password, appearance, and two-factor.</p>
    </a>
    <a href="{{ route('teams.index') }}" class="rounded-xl border border-line bg-surface p-5 transition hover:border-brand">
      <h2 class="font-semibold">Teams</h2>
      <p class="mt-1 text-sm text-muted">Create, switch, invite, and manage members.</p>
    </a>
  </div>
@endsection
