@extends('layouts.guest')

@section('title', 'Two-factor — {{ app_display }}')

@section('content')
  <h1 class="font-display text-2xl font-semibold tracking-tight">Two-factor challenge</h1>
  <p class="mt-1 text-sm text-muted">Enter an authenticator code or a recovery code.</p>
  @isset(error)
    <p class="mt-4 rounded-lg border border-red-200 bg-red-50 px-3 py-2 text-sm text-red-800" role="alert">{{ error }}</p>
  @endisset
  <form method="post" action="{{ form_action }}" class="mt-6 space-y-4">
    @csrf
    <label class="block text-sm font-medium">Authentication code
      <input type="text" name="code" inputmode="numeric" autocomplete="one-time-code"
             class="mt-1 w-full rounded-lg border border-line bg-paper px-3 py-2 focus:border-brand focus:outline-none focus:ring-2 focus:ring-brand/25"/>
    </label>
    <p class="text-center text-xs text-muted">or</p>
    <label class="block text-sm font-medium">Recovery code
      <input type="text" name="recovery_code"
             class="mt-1 w-full rounded-lg border border-line bg-paper px-3 py-2 focus:border-brand focus:outline-none focus:ring-2 focus:ring-brand/25"/>
    </label>
    <button type="submit" class="w-full rounded-lg bg-brand px-4 py-2.5 text-sm font-semibold text-white hover:bg-brand-glow">Continue</button>
  </form>
@endsection
