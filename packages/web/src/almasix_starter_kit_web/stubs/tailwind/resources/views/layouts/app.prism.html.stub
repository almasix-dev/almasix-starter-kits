<!DOCTYPE html>
<html lang="en" class="h-full">
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1"/>
  <title>@yield('title', '{{ app_display }}')</title>
  <link rel="preconnect" href="https://fonts.googleapis.com"/>
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin/>
  <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;500;600&family=Outfit:wght@400;500;600;700;800&display=swap" rel="stylesheet"/>
  <script>
    (function(){try{var m=localStorage.getItem('forge-theme')||'system';var d=m==='dark'||(m==='system'&&matchMedia('(prefers-color-scheme: dark)').matches);document.documentElement.classList.toggle('dark',d);}catch(e){}})();
  </script>
  @vite(['resources/css/app.css', 'resources/js/app.js'])
  @conduitScripts
</head>
<body class="min-h-full bg-paper text-ink antialiased font-sans">
  <div class="flex min-h-screen">
    <aside class="hidden w-60 shrink-0 flex-col border-r border-line bg-surface md:flex">
      <div class="flex h-14 items-center gap-2 border-b border-line px-5">
        <span class="inline-block h-2.5 w-2.5 rounded-full bg-brand shadow-[0_0_12px_var(--color-brand-glow)]"></span>
        <a href="{{ route('dashboard') }}" class="font-display text-lg font-semibold tracking-tight">{{ name }}</a>
      </div>
      <nav class="flex flex-1 flex-col gap-0.5 p-3 text-sm font-medium">
        <a href="{{ route('dashboard') }}" class="rounded-lg px-3 py-2 text-ink hover:bg-paper @if(route_is('dashboard')) bg-paper text-brand @endif">Dashboard</a>
        <a href="{{ route('notifications.index') }}" class="rounded-lg px-3 py-2 text-muted hover:bg-paper hover:text-ink @if(route_is('notifications.*')) bg-paper text-brand @endif">Notifications</a>
        <a href="{{ route('profile.show') }}" class="rounded-lg px-3 py-2 text-muted hover:bg-paper hover:text-ink @if(route_is('profile.*') or route_is('user-password.*') or route_is('two-factor.*') or route_is('appearance.*')) bg-paper text-brand @endif">Settings</a>
        <a href="{{ route('teams.index') }}" class="rounded-lg px-3 py-2 text-muted hover:bg-paper hover:text-ink @if(route_is('teams.*')) bg-paper text-brand @endif">Teams</a>
      </nav>
      <div class="border-t border-line p-3 text-xs text-muted">Forge · {{ name }}</div>
    </aside>

    <div class="flex min-w-0 flex-1 flex-col">
      <header class="flex h-14 items-center justify-between gap-4 border-b border-line bg-surface/80 px-4 backdrop-blur md:px-6">
        <div class="flex items-center gap-3 md:hidden">
          <a href="{{ route('dashboard') }}" class="font-display font-semibold">{{ name }}</a>
        </div>
        <div class="ml-auto flex items-center gap-3">
          @conduit('theme_toggle')
          <details class="relative">
            <summary class="cursor-pointer list-none rounded-lg border border-line bg-paper px-3 py-1.5 text-sm font-medium hover:border-brand">
              Account
            </summary>
            <div class="absolute right-0 z-20 mt-2 w-44 rounded-xl border border-line bg-surface py-1 text-sm shadow-lg">
              <a class="block px-3 py-2 hover:bg-paper" href="{{ route('profile.show') }}">Profile</a>
              <a class="block px-3 py-2 hover:bg-paper" href="{{ route('appearance.edit') }}">Appearance</a>
              <form method="post" action="{{ route('logout') }}" class="border-t border-line">
                @csrf
                <button type="submit" class="block w-full px-3 py-2 text-left text-red-600 hover:bg-paper">Log out</button>
              </form>
            </div>
          </details>
        </div>
      </header>

      <main class="flex-1 px-4 py-8 md:px-8">
        <div class="mx-auto max-w-5xl">
          @yield('content')
        </div>
      </main>
    </div>
  </div>
</body>
</html>
