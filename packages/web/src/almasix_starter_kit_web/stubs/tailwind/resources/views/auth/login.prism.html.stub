@extends('layouts.guest')

@section('title', 'Log in — {{ app_display }}')

@section('content')
  <h1 class="font-display text-2xl font-semibold tracking-tight">Welcome back</h1>
  <p class="mt-1 text-sm text-muted">Sign in to {{ name }}.</p>
  @isset(error)
    <p class="mt-4 rounded-lg border border-red-200 bg-red-50 px-3 py-2 text-sm text-red-800" role="alert">{{ error }}</p>
  @endisset
  @isset(status)
    <p class="mt-4 rounded-lg border border-emerald-200 bg-emerald-50 px-3 py-2 text-sm text-emerald-800" role="status">{{ status }}</p>
  @endisset
  <form method="post" action="{{ form_action }}" class="mt-6 space-y-4">
    @csrf
    <label class="block text-sm font-medium">Email
      <input type="email" name="email" required autocomplete="username"
             class="mt-1 w-full rounded-lg border border-line bg-paper px-3 py-2 shadow-sm focus:border-brand focus:outline-none focus:ring-2 focus:ring-brand/25"/>
    </label>
    <label class="block text-sm font-medium">Password
      <input type="password" name="password" required autocomplete="current-password"
             class="mt-1 w-full rounded-lg border border-line bg-paper px-3 py-2 shadow-sm focus:border-brand focus:outline-none focus:ring-2 focus:ring-brand/25"/>
    </label>
    <div class="flex items-center justify-between text-sm">
      <label class="flex items-center gap-2 text-muted">
        <input type="checkbox" name="remember" value="1" class="rounded border-line text-brand focus:ring-brand"/>
        Remember me
      </label>
      <a href="{{ forgot_url }}" class="font-medium text-brand hover:underline">Forgot password?</a>
    </div>
    <button type="submit" class="w-full rounded-lg bg-brand px-4 py-2.5 text-sm font-semibold text-white hover:bg-brand-glow">Log in</button>
  </form>
  <p class="mt-6 text-center text-sm text-muted">No account? <a class="font-medium text-brand hover:underline" href="{{ register_url }}">Sign up</a></p>
@endsection
