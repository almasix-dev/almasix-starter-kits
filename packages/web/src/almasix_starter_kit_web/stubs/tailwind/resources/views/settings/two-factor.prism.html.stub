@extends('layouts.app')

@section('title', 'Two-factor — {{ app_display }}')

@section('content')

  <nav class="mb-8 flex flex-wrap gap-2 text-sm font-medium">
    <a href="{{ route('profile.show') }}" class="rounded-lg px-3 py-1.5 hover:bg-surface border border-transparent hover:border-line">Profile</a>
    <a href="{{ route('user-password.edit') }}" class="rounded-lg px-3 py-1.5 hover:bg-surface border border-transparent hover:border-line">Password</a>
    <a href="{{ route('two-factor.show') }}" class="rounded-lg px-3 py-1.5 hover:bg-surface border border-transparent hover:border-line">Two-factor</a>
    <a href="{{ route('appearance.edit') }}" class="rounded-lg px-3 py-1.5 hover:bg-surface border border-transparent hover:border-line">Appearance</a>
  </nav>

  <h1 class="font-display text-2xl font-semibold tracking-tight">Two-factor authentication</h1>
  <p class="mt-1 text-sm text-muted">Add an authenticator app for stronger sign-in.</p>
  @isset(status)
    <p class="mt-4 rounded-lg border border-emerald-200 bg-emerald-50 px-3 py-2 text-sm text-emerald-800" role="status">{{ status }}</p>
  @endisset
  @isset(error)
    <p class="mt-4 rounded-lg border border-red-200 bg-red-50 px-3 py-2 text-sm text-red-800" role="alert">{{ error }}</p>
  @endisset

  @if(enabled)
    <p class="mt-6 text-sm font-medium text-brand">Two-factor is enabled.</p>
    <form method="post" action="{{ disable_url }}" class="mt-4">
      @csrf
      {!! method_field('DELETE') !!}
      <button type="submit" class="rounded-lg border border-line px-4 py-2 text-sm font-medium hover:border-red-400 hover:text-red-600">Disable</button>
    </form>
    <form method="post" action="{{ recovery_url }}" class="mt-3">
      @csrf
      <button type="submit" class="text-sm font-medium text-brand hover:underline">Regenerate recovery codes</button>
    </form>
  @elseif(pending)
    <div class="mt-6 max-w-lg space-y-4">
      <p class="text-sm text-muted">Scan this QR code with your authenticator app, then confirm with a code.</p>
      @isset(qr_svg)
        <div class="inline-block rounded-xl border border-line bg-white p-3">{!! qr_svg !!}</div>
      @endisset
      <p class="text-xs text-muted">Secret: <code>{{ secret }}</code></p>
      @isset(qr_uri)
        <details class="text-sm text-muted">
          <summary class="cursor-pointer">Can't scan? Show setup URI</summary>
          <code class="mt-2 block break-all rounded-lg border border-line bg-paper p-3 text-xs text-ink">{{ qr_uri }}</code>
        </details>
      @endisset
      <form method="post" action="{{ confirm_url }}" class="space-y-3">
        @csrf
        <input type="text" name="code" required placeholder="6-digit code" class="w-full rounded-lg border border-line bg-surface px-3 py-2"/>
        <button type="submit" class="rounded-lg bg-brand px-4 py-2 text-sm font-semibold text-white">Confirm</button>
      </form>
    </div>
  @else
    <form method="post" action="{{ enable_url }}" class="mt-6">
      @csrf
      <button type="submit" class="rounded-lg bg-brand px-4 py-2 text-sm font-semibold text-white hover:bg-brand-glow">Enable two-factor</button>
    </form>
  @endif

  @isset(recovery_codes)
    <div class="mt-8 max-w-lg rounded-xl border border-line bg-surface p-5">
      <h2 class="font-semibold">Recovery codes</h2>
      <p class="mt-1 text-sm text-muted">Store these somewhere safe. Each code works once.</p>
      <ul class="mt-3 grid grid-cols-2 gap-2 font-mono text-sm">
        @foreach(recovery_codes as code)
          <li class="rounded bg-paper px-2 py-1">{{ code }}</li>
        @endforeach
      </ul>
    </div>
  @endisset
@endsection
