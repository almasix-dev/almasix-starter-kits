@extends('layouts.app')

@section('title', 'Password — {{ app_display }}')

@section('content')

  <nav class="mb-8 flex flex-wrap gap-2 text-sm font-medium">
    <a href="{{ route('profile.show') }}" class="rounded-lg px-3 py-1.5 hover:bg-surface border border-transparent hover:border-line">Profile</a>
    <a href="{{ route('user-password.edit') }}" class="rounded-lg px-3 py-1.5 hover:bg-surface border border-transparent hover:border-line">Password</a>
    <a href="{{ route('two-factor.show') }}" class="rounded-lg px-3 py-1.5 hover:bg-surface border border-transparent hover:border-line">Two-factor</a>
    <a href="{{ route('appearance.edit') }}" class="rounded-lg px-3 py-1.5 hover:bg-surface border border-transparent hover:border-line">Appearance</a>
  </nav>

  <h1 class="font-display text-2xl font-semibold tracking-tight">Password</h1>
  <p class="mt-1 text-sm text-muted">Ensure your account uses a long, random password.</p>
  @isset(status)
    <p class="mt-4 rounded-lg border border-emerald-200 bg-emerald-50 px-3 py-2 text-sm text-emerald-800" role="status">{{ status }}</p>
  @endisset
  @isset(error)
    <p class="mt-4 rounded-lg border border-red-200 bg-red-50 px-3 py-2 text-sm text-red-800" role="alert">{{ error }}</p>
  @endisset
  <form method="post" action="{{ form_action }}" class="mt-8 max-w-lg space-y-4">
    @csrf
    {!! method_field('PUT') !!}
    <label class="block text-sm font-medium">Current password
      <input type="password" name="current_password" required class="mt-1 w-full rounded-lg border border-line bg-surface px-3 py-2 focus:border-brand focus:outline-none focus:ring-2 focus:ring-brand/25"/>
    </label>
    <label class="block text-sm font-medium">New password
      <input type="password" name="password" required class="mt-1 w-full rounded-lg border border-line bg-surface px-3 py-2 focus:border-brand focus:outline-none focus:ring-2 focus:ring-brand/25"/>
    </label>
    <label class="block text-sm font-medium">Confirm password
      <input type="password" name="password_confirmation" required class="mt-1 w-full rounded-lg border border-line bg-surface px-3 py-2 focus:border-brand focus:outline-none focus:ring-2 focus:ring-brand/25"/>
    </label>
    <button type="submit" class="rounded-lg bg-brand px-4 py-2 text-sm font-semibold text-white hover:bg-brand-glow">Update password</button>
  </form>
@endsection
