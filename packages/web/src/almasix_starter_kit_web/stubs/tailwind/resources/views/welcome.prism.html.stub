@extends('layouts.marketing')

@section('title', '{{ app_display }}')

@section('content')
  <section class="relative min-h-screen overflow-hidden forge-mesh">
    <div class="pointer-events-none absolute inset-0 forge-drift opacity-90" aria-hidden="true">
      <div class="absolute -left-24 top-1/4 h-72 w-72 rounded-full bg-brand-glow/20 blur-3xl"></div>
      <div class="absolute right-0 top-0 h-96 w-96 rounded-full bg-brand/25 blur-3xl"></div>
      <div class="absolute bottom-10 left-1/3 h-64 w-64 rounded-full bg-accent/20 blur-3xl dark:bg-accent/10"></div>
    </div>

    <div class="relative z-10 mx-auto flex min-h-screen max-w-6xl flex-col px-6 pb-16 pt-8 sm:px-10">
      <header class="flex items-center justify-between forge-rise">
        <p class="font-display text-2xl font-semibold tracking-tight text-ink dark:text-white sm:text-3xl">{{ name }}</p>
        <nav class="flex items-center gap-3 text-sm font-medium">
          @guest
            <a href="{{ login_url }}" class="text-ink/70 hover:text-ink dark:text-white/70 dark:hover:text-white">Log in</a>
          @else
            <a href="{{ dashboard_url }}" class="text-ink/70 hover:text-ink dark:text-white/70 dark:hover:text-white">Dashboard</a>
          @endguest
          <button type="button" onclick="window.__forgeSetTheme && window.__forgeSetTheme(document.documentElement.classList.contains('dark') ? 'light' : 'dark')"
                  class="rounded-full border border-ink/15 px-3 py-1 text-xs text-ink/70 hover:border-brand dark:border-white/20 dark:text-white/70">Theme</button>
        </nav>
      </header>

      <div class="flex flex-1 flex-col justify-center py-20">
        @isset(status)
          <p class="mb-6 max-w-lg rounded-lg border border-emerald-400/40 bg-emerald-500/10 px-4 py-3 text-sm text-emerald-800 dark:text-emerald-200" role="status">{{ status }}</p>
        @endisset

        <h1 class="forge-rise-delay font-display max-w-3xl text-5xl font-semibold leading-[1.05] tracking-tight text-ink dark:text-white sm:text-6xl lg:text-7xl">
          Ship the product<br class="hidden sm:block"/> you already see.
        </h1>
        <p class="forge-rise-delay-2 mt-6 max-w-xl text-lg text-ink/70 dark:text-white/65 sm:text-xl">
          {{ name }} is ready — auth, teams, and a calm workspace forged for people who build.
        </p>
        <div class="forge-rise-delay-2 mt-10 flex flex-wrap items-center gap-3">
          @guest
            <a href="{{ register_url }}"
               class="inline-flex items-center rounded-full bg-accent px-6 py-3 text-sm font-semibold text-ink shadow-[0_0_0_1px_rgba(10,15,26,0.08)] transition hover:brightness-105">
              Get started
            </a>
            <a href="{{ login_url }}"
               class="inline-flex items-center rounded-full border border-ink/20 bg-white/40 px-6 py-3 text-sm font-semibold text-ink backdrop-blur hover:border-brand dark:border-white/25 dark:bg-white/5 dark:text-white">
              Log in
            </a>
          @else
            <a href="{{ dashboard_url }}"
               class="inline-flex items-center rounded-full bg-accent px-6 py-3 text-sm font-semibold text-ink transition hover:brightness-105">
              Open dashboard
            </a>
          @endguest
        </div>
      </div>
    </div>
  </section>
@endsection
