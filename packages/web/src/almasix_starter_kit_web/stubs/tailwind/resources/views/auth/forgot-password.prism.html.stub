@extends('layouts.guest')

@section('title', 'Forgot password — {{ app_display }}')

@section('content')
  <h1 class="font-display text-2xl font-semibold tracking-tight">Forgot password</h1>
  <p class="mt-1 text-sm text-muted">We will email you a reset link.</p>
  @isset(error)
    <p class="mt-4 rounded-lg border border-red-200 bg-red-50 px-3 py-2 text-sm text-red-800" role="alert">{{ error }}</p>
  @endisset
  @isset(status)
    <p class="mt-4 rounded-lg border border-emerald-200 bg-emerald-50 px-3 py-2 text-sm text-emerald-800" role="status">{{ status }}</p>
  @endisset
  <form method="post" action="{{ form_action }}" class="mt-6 space-y-4">
    @csrf
    <label class="block text-sm font-medium">Email
      <input type="email" name="email" required
             class="mt-1 w-full rounded-lg border border-line bg-paper px-3 py-2 focus:border-brand focus:outline-none focus:ring-2 focus:ring-brand/25"/>
    </label>
    <button type="submit" class="w-full rounded-lg bg-brand px-4 py-2.5 text-sm font-semibold text-white hover:bg-brand-glow">Email reset link</button>
  </form>
  <p class="mt-6 text-center text-sm"><a class="text-brand hover:underline" href="{{ login_url }}">Back to login</a></p>
@endsection
