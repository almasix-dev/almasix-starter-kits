@extends('layouts.app')

@section('title', 'Create team — {{ app_display }}')

@section('content')
  <h1 class="font-display text-2xl font-semibold tracking-tight">Create team</h1>
  @isset(error)
    <p class="mt-4 rounded-lg border border-red-200 bg-red-50 px-3 py-2 text-sm text-red-800" role="alert">{{ error }}</p>
  @endisset
  <form method="post" action="{{ form_action }}" class="mt-8 max-w-md space-y-4">
    @csrf
    <label class="block text-sm font-medium">Team name
      <input type="text" name="name" required class="mt-1 w-full rounded-lg border border-line bg-surface px-3 py-2 focus:border-brand focus:outline-none focus:ring-2 focus:ring-brand/25"/>
    </label>
    <button type="submit" class="rounded-lg bg-brand px-4 py-2 text-sm font-semibold text-white hover:bg-brand-glow">Create</button>
  </form>
@endsection
