@extends('layouts.guest')
@section('title', 'Two-factor — {{ app_display }}')
@section('content')
  <h1 class="h4 font-display fw-semibold">Two-factor challenge</h1>
  <p class="small text-secondary">Enter an authenticator code or a recovery code.</p>
  @isset(error)<div class="alert alert-danger py-2">{{ error }}</div>@endisset
  <form method="post" action="{{ form_action }}" class="mt-3">
    @csrf
    <div class="mb-3"><label class="form-label">Authentication code</label><input type="text" name="code" class="form-control" inputmode="numeric"/></div>
    <p class="text-center small text-secondary">or</p>
    <div class="mb-3"><label class="form-label">Recovery code</label><input type="text" name="recovery_code" class="form-control"/></div>
    <button type="submit" class="btn btn-brand w-100">Continue</button>
  </form>
@endsection
