@extends('layouts.app')
@section('title', 'Notifications — {{ app_display }}')
@section('content')
  <div class="d-flex justify-content-between align-items-center">
    <div><h1 class="font-display h3 fw-semibold mb-0">Notifications</h1><p class="small text-secondary mb-0">Your database notification inbox.</p></div>
    <form method="post" action="{{ mark_all_url }}">@csrf<button class="btn btn-outline-secondary btn-sm" type="submit">Mark all read</button></form>
  </div>
  @isset(status)<div class="alert alert-success py-2 mt-3">{{ status }}</div>@endisset
  @if(empty)
    <div class="border border-dashed rounded-3 text-center py-5 mt-4">
      <p class="font-display fs-5 fw-semibold mb-1">You're all caught up</p>
      <p class="small text-secondary mb-0">When something needs your attention, it will show up here.</p>
    </div>
  @else
    <ul class="list-group mt-4">
      @foreach(notifications as note)
        <li class="list-group-item"><strong>{{ note['type'] }}</strong><div class="small text-secondary">{{ note['data'] }}</div></li>
      @endforeach
    </ul>
  @endif
@endsection
