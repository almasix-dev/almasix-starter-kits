@extends('layouts.guest')
@section('title', 'Verify email — {{ app_display }}')
@section('content')
  <h1 class="h4 font-display fw-semibold">Verify your email</h1>
  <p class="small text-secondary">Please confirm your email address before continuing.</p>
  @isset(status)<div class="alert alert-success py-2">{{ status }}</div>@endisset
  <form method="post" action="{{ resend_url }}" class="mt-3">@csrf<button class="btn btn-brand w-100" type="submit">Resend verification email</button></form>
  <form method="post" action="{{ logout_url }}" class="mt-2">@csrf<button class="btn btn-link btn-sm w-100" type="submit">Log out</button></form>
@endsection
