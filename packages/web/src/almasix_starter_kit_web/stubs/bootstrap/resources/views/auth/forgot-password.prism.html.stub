@extends('layouts.guest')
@section('title', 'Forgot password — {{ app_display }}')
@section('content')
  <h1 class="h4 font-display fw-semibold">Forgot password</h1>
  <p class="small text-secondary">We will email you a reset link.</p>
  @isset(error)<div class="alert alert-danger py-2">{{ error }}</div>@endisset
  @isset(status)<div class="alert alert-success py-2">{{ status }}</div>@endisset
  <form method="post" action="{{ form_action }}" class="mt-3">
    @csrf
    <div class="mb-3"><label class="form-label">Email</label><input type="email" name="email" required class="form-control"/></div>
    <button type="submit" class="btn btn-brand w-100">Email reset link</button>
  </form>
  <p class="text-center small mt-3 mb-0"><a href="{{ login_url }}">Back to login</a></p>
@endsection
