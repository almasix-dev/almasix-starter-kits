name: RFC
description: Design discussion before a large change.
title: "RFC: "
labels: ["rfc", "needs-triage"]
body:
  - type: textarea
    id: proposal
    attributes:
      label: Proposal
      description: Why, what, and open questions. Keep it actionable — decisions to lock, not a questionnaire.
      render: markdown
    validations:
      required: true
